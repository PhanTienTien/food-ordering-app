name: foodapp
description: "A new Flutter project."
publish_to: 'none'

dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  freezed:
  json_serializable:

  # State management
  flutter_riverpod: ^2.5.1

  # Routing (web + mobile rất ngon)
  go_router: ^14.2.0

  # HTTP client
  dio: ^5.4.0

  # JSON
  json_annotation: ^4.9.0

  # Utils
  freezed_annotation: ^2.4.1

dev_dependencies:
  build_runner:
  freezed:
  json_serializable:

environment:
  sdk: '>=3.11.0 <4.0.0'
