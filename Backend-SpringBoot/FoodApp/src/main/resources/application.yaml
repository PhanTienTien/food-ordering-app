server:
  port: 8082

spring:
  application:
    name: FoodApp

  datasource:
    url: jdbc:mysql://localhost:3306/food_ordering_app
    username: root
    password: Tien@260704
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
